# clipboard-to-file-input

Intercept the clipboard paste event and add the files to a file input.

Allows using selector or a file element to perform its task.

### Demo
https://clipboard-to-file-input.netlify.app/
